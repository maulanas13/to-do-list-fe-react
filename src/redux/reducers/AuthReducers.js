const INITIAL_STATE = {
  id: null,
  username: "",
  email: "",
  role_id: null,
  is_login: null,
  is_verified: null,
};

const AuthReducers = (state = INITIAL_STATE, action) => {
  switch (type) {
    case value:
      return;

    default:
      return state;
  }
};
